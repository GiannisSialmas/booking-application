[project]
name = "booking"
version = "0.1.0"
description = "Booking service -- owns seat inventory, reservations, and payments."
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
